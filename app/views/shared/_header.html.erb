<nav class="navbar navbar-default navbar-static-top" role="navigation">
	 	<% if user_signed_in? %>
	 		<%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn btn-primary pull-right b" %>
	 		<%= link_to 'Categories', categories_path, class: "btn btn-primary pull-right b" %>
		<% else %>
	 		<%= link_to 'Categories', categories_path, class: "btn btn-primary pull-right b" %>
	 	<% end %>
	 	<h4>Basic Shop</h4>
</nav>